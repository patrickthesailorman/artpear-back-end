// https://semaphoreci.com/community/tutorials/getting-started-with-node-js-and-mocha
var expect  = require("chai").expect;
var artPear = require(' '); //// include the file we want to test
describe("Art Pear Testing", function(){
  describe("Model", function(){
    it("Populates with all fields", function(){
      //// add what we need to expect here
      var model = //////
      expect(model).to.have.lengthOf(#);///// add number we want expect it should have.

    });
  });
});
